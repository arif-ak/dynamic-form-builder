{% extends 'base.html.twig' %}

{% block title %}Logged in{% endblock %}

{% block body %}
    <h3> Welcome to form builder application (pre-release version) ,  {{ user.firstName}} <h3>
    <hr/>

    <div class="form-row text-center">  
        <div class="col-8">
            <h3> Select from given roles to go through the application -> : <h3>
        </div>
        <div class="col-2">
            <a class="btn btn-sm btn-primary" href="{{ path('dynamic_form_index') }}">View as admin</a>
        </div>
        <div class="col-2">
            <a class="btn btn-sm btn-primary" href="{{ path('dynamic_form_index_user') }}">View as user</a>
        </div>
    </div>
{% endblock %}
